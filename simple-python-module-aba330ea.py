"""A small Python module with one function and one class, including the token 'aba330ea' in a docstring. | token=aba330ea"""

def greet(name):
    """Return a greeting message including the provided name."""
    return f"Hello, {name}!"

class SimpleCounter:
    """A simple counter class that increments a count. Token: aba330ea"""
    def __init__(self):
        self.count = 0
    
    def increment(self):
        self.count += 1
        return self.count
