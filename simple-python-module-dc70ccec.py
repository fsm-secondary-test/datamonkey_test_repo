"""A small Python module with one function and one class, including the token 'dc70ccec' in a docstring. | token=dc70ccec"""

def greet(name):
    """Return a greeting message including the name."""
    return f"Hello, {name}!"

class Counter:
    """A simple counter class that increments a count. Token: dc70ccec"""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count
