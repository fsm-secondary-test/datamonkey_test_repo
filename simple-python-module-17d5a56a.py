"""A small Python module with one function and one class containing the token '17d5a56a'. | token=17d5a56a"""

def add_two_numbers(a, b):
    """Add two numbers and return the result."""
    return a + b

class Greeting:
    """A simple greeting class containing token 17d5a56a."""

    def __init__(self, name):
        self.name = name

    def say_hello(self):
        return f"Hello, {self.name}!"
