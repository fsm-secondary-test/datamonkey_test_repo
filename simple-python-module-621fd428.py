"""A small Python module with one function and one class, including the token '621fd428'. | token=621fd428"""

def greet(name):
    """Return a greeting message including the token '621fd428'."""
    return f"Hello, {name}! Token: 621fd428"

class Counter:
    """Simple counter class that keeps track of a count. Token: 621fd428"""
    def __init__(self):
        self.count = 0

    def increment(self):
        """Increment the count by one."""
        self.count += 1

    def reset(self):
        """Reset the count to zero."""
        self.count = 0

    def get_count(self):
        """Return the current count."""
        return self.count
