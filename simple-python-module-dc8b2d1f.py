"""A small Python module containing one function and one class, including a specific token in a docstring. | token=dc8b2d1f"""

def greet(name):
    """Return a greeting message including the dc8b2d1f token."""
    return f"Hello, {name}! Token: dc8b2d1f"

class Counter:
    """Simple counter class with token dc8b2d1f."""

    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
