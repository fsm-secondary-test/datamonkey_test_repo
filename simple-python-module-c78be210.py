"""A small Python module with one function and one class, including the token 'c78be210'. | token=c78be210"""

def greet(name: str) -> str:
    """Return a greeting message including the token c78be210."""
    return f"Hello, {name}! Token: c78be210"

class Counter:
    """Simple counter class with token c78be210 in docstring."""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
