"""A Python module containing a simple function and a simple class. Includes the token '2ec05485' in a docstring. | token=2ec05485"""

def greet(name):
    """Return a greeting message including the provided name."""
    return f"Hello, {name}!"

class Counter:
    """A simple counter class with token 2ec05485."""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
