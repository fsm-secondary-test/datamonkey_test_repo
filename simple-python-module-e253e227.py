"""A small Python module with one function and one class, including the token 'e253e227' in a docstring. | token=e253e227"""

def greet(name):
    """Return a greeting message including the given name and token e253e227."""
    return f"Hello, {name}! Token: e253e227"

class Counter:
    """Simple counter class that counts up from zero. Token: e253e227"""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
