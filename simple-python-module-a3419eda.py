"""A small Python module containing one simple function and one simple class. Includes the token 'a3419eda' in a docstring. | token=a3419eda"""

def greet(name):
    """Return a greeting message including the given name. Token: a3419eda"""
    return f"Hello, {name}!"

class Counter:
    """Simple counter class that increments by one. Token: a3419eda"""

    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count
