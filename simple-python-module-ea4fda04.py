"""A small Python module containing one function and one class, including the token 'ea4fda04' in a docstring. | token=ea4fda04"""

def greet(name):
    """Return a greeting message including the name provided."""
    return f"Hello, {name}!"

class Counter:
    """A simple counter class that increments and returns the count. Token: ea4fda04"""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count
