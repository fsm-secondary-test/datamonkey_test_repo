"""A simple Python module containing one function and one class with the token '498fce2b' in a docstring. | token=498fce2b"""

def greet(name):
    """Return a greeting message including the given name."""
    return f"Hello, {name}!"

class Counter:
    """Simple counter class with token 498fce2b in docstring."""

    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count
