"""A small Python module with one function and one class, including the token '5fb1df0a'. | token=5fb1df0a"""

def greet(name):
    """Return a greeting message including the token 5fb1df0a."""
    return f"Hello, {name}! Your token is 5fb1df0a."

class Counter:
    """A simple counter class that uses token 5fb1df0a in its docstring."""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
