"""A small Python module containing one simple function and one simple class with the token '7559e6e0' in a docstring. | token=7559e6e0"""

def greet(name):
    """Return a greeting message with the given name."""
    return f"Hello, {name}!"

class Counter:
    """A simple counter class with token 7559e6e0 in the docstring."""

    def __init__(self):
        self.count = 0

    def increment(self):
        """Increase the count by one."""
        self.count += 1

    def get_count(self):
        """Return the current count."""
        return self.count
