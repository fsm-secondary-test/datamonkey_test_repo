"""A small Python module containing one function and one class with the token 'e07160b2' included in a docstring. | token=e07160b2"""

def greet(name):
    """Return a greeting message including the name."""
    return f"Hello, {name}!"

class Counter:
    """A simple counter class that increments a count. Token: e07160b2"""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count
