"""A small Python module including one function and one class with the token '257a5e64' in a docstring. | token=257a5e64"""

def greet(name):
    """Return a greeting string with the given name."""
    return f"Hello, {name}!"

class SimpleCounter:
    """Simple counter class including token 257a5e64 in docstring."""
    def __init__(self):
        self.count = 0

    def increment(self):
        self.count += 1
        return self.count

    def reset(self):
        self.count = 0
